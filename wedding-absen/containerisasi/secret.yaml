apiVersion: v1
kind: Secret
metadata:
  name: wedding-absen-secret
type: Opaque
stringData:
  # These values are public after the frontend loads. Do not add server secrets here.
  VITE_SUPABASE_URL: "https://your-project-id.supabase.co"
  VITE_SUPABASE_PUBLISHABLE_KEY: "replace-with-publishable-key"
